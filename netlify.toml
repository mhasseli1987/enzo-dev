[build]
  command = "rm -rf dist && mkdir -p dist && cp index.html dist/ && cp -r css js frames dist/"
  publish = "dist"

[[headers]]
  for = "/frames/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
